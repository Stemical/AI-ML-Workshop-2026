[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "johnkk-spam-detector"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["scikit-learn"]

[tool.setuptools.package-data]
john = ["*.pkl"]
